[package]
name = "rustref"
version = "0.1.0"
edition = "2021"

[dependencies]
syn = { version = "2", features = ["full", "parsing", "printing", "visit"] }
quote = "1"
serde_json = "1"

[profile.release]
debug = false
